export const FILE_NAME = 'Calendar.tsx'

export const SNIPPET_HINT =
  'The board on the Calendar tab, as one file. It reads the same spec, the same events and the same row height, and it draws them with the same Tailwind classes — the dark half of every pair is a dark: variant, so it follows the system in a stock Tailwind v4 project. Install @midstem/chronous and @midstem/chronous-react, paste, render.'

export const COPY_LABEL = 'Copy'

export const COPIED_LABEL = 'Copied'

export const COPIED_MS = 1600

export const SPEC_INDENT = '  '

export const HOURS_IN_DAY = 24

export const KEY_PATTERN = /"([A-Za-z][\w]*)":/g

export const KEY_REPLACEMENT = '$1:'
