export const MAX_JSON_LENGTH = 40000

export const TRUNCATION_NOTICE = '\n\n… truncated by the playground'

export const STATE_HINT =
  'Everything buildCalendar returns is a scalar — this is the JSON a server could have sent.'
