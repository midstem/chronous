{
  "name": "@midstem/chronous",
  "version": "0.0.0",
  "type": "module",
  "license": "MIT",
  "description": "Headless scheduling engine: time model, layout and recurrence. No React, no DOM",
  "author": "Midstem",
  "sideEffects": false,
  "peerDependencies": {
    "temporal-polyfill": ">=0.3.0"
  },
  "peerDependenciesMeta": {
    "temporal-polyfill": {
      "optional": true
    }
  },
  "imports": {
    "#src/*": "./src/*/index.ts"
  },
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist",
    "LICENSE",
    "README.md"
  ],
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/midstem/chronous.git",
    "directory": "packages/core"
  },
  "bugs": {
    "url": "https://github.com/midstem/chronous/issues"
  },
  "homepage": "https://github.com/midstem/chronous#readme",
  "keywords": [
    "calendar",
    "scheduler",
    "headless",
    "timezone",
    "recurrence",
    "typescript"
  ],
  "scripts": {
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest",
    "test:polyfill": "vitest run",
    "test:native": "CHRONOUS_TEMPORAL=native vitest run",
    "test:run": "npm run test:polyfill && npm run test:native",
    "test:coverage": "vitest run --coverage",
    "bench": "vitest bench --run",
    "bench:save": "vitest bench --run --outputJson=bench.json",
    "bench:compare": "vitest bench --run --compare=bench.json",
    "bench:native": "CHRONOUS_TEMPORAL=native vitest bench --run",
    "prepack": "node ../../tools/scripts/copy-license.mjs"
  }
}
